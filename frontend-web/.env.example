# Dev local (browser pe localhost): API backend
VITE_API_BASE_URL=http://localhost:4001

# Opțional (legacy)
# VITE_API_URL=http://localhost:4000/api/v1

# Producție: pe host ≠ localhost, originea API vine din `window.__APP_CONFIG__.backend_domain`
# (script runtime generat de sysadmin, mapat din variabila Docker `backend_domain`).
